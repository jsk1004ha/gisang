experiment:
  name: real_2024q4_2025q1_richer_multitarget
training:
  batch_size: 64
  max_epochs: 30
  early_stopping_patience: 5
  num_workers: 0
  device: cuda
artifacts:
  root_dir: data/artifacts/experiments
