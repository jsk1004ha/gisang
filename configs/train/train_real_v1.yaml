experiment:
  name: real_v1_seoul_temperature
training:
  batch_size: 64
  max_epochs: 5
  early_stopping_patience: 2
  num_workers: 0
  device: cpu
artifacts:
  root_dir: data/artifacts/experiments
seed: 42
